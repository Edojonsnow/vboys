import { categoryType } from "./categoryType";
import { eventInfoType } from "./eventInfoType";
import { imageType } from "./imageType";

export const schema = {
  types: [imageType, categoryType, eventInfoType],
};
